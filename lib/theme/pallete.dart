import 'package:flutter/material.dart';

class Pallete {
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;

  // swim pool color
  static const swimBlue = Color.fromRGBO(54, 47, 217, 1.0);
  static const swimBlueLight = Color.fromRGBO(133, 205, 253, 1.0);

  // like button
  static const likeBackground = Color.fromRGBO(222, 252, 249, 1.0);
  static const likefill = Color.fromRGBO(60, 132, 171, 1.0);
  static const likefill2 = Color.fromRGBO(133, 205, 253, 1.0);
}
